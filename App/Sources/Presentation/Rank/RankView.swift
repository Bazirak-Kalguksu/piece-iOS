import SwiftUI

struct RankView: View {
    var body: some View {
        VStack(spacing: 0) {
            Text("RANK")
                .font(.partial(size: 28))
                .foregroundStyle(Color.Blue.blue600)
            
            ScrollView {
                
            }
        }
    }
}

#Preview {
    
        RankView()
    
}
